/**
 * Bank Payment Parser Global Scripts (Desk)
 *
 * NOTE:
 * - `frappe.ready` is only defined on website pages, not on the Desk.
 * - This file is included via `app_include_js`, which is Desk-only.
 *
 * If you need website-specific logic, create a separate JS file and
 * include it via `web_include_js` in `hooks.py`.
 */

// Currently no global Desk-level hooks are required.
// This file is kept as a placeholder for future extensions.
